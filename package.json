{
  "name": "root-dirs",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run -s clean && tsgo",
    "clean": "rm -rf dist/"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260428.1"
  }
}
